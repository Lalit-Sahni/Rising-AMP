# Design reference

`opal-track-redesign.html` is the Phase 2 look source of truth. Open it in a browser.

It is a static concept mockup, not the live app. Match its tokens and treatments. Do not paste it into React. Do not build mockup-only features (email login, new job list, Reports page). See `PHASE2.md`.
